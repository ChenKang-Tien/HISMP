# 血液透析臨床資訊管理系統 (Dialysis Clinic HIS/EMR System) 專案深度分析報告

本專案是一個專為**血液透析（洗腎）診所**量身打造的現代化臨床資訊系統（HIS / EMR）。系統採用前後端分離架構，後端基於 **Laravel 13** (PHP 8.3+) 提供安全、嚴謹的 RESTful API，前端則基於 **Vue 3 (Composition API)**、**Element Plus** 及 **Tailwind CSS v4.0** 構建流暢、直觀的高效操作介面。

本報告針對專案的技術架構、核心業務流程、API 路由、資料庫領域模型以及系統亮點進行全方位的深度分析。

---

## 📂 一、 技術棧與架構分析

### 1. 後端架構 (Laravel 13 & PHP 8.3+)
* **高效能與強型別**：專案基於最新的 PHP 8.3+ 與 Laravel 13 構建，保障了後端執行速度並能充分利用強型別、唯讀屬性等現代 PHP 特性。
* **安全性與身份驗證**：整合 **Laravel Sanctum** 提供輕量級且高安全性的 Token 驗證機制。
* **細粒度權限控制 (RBAC)**：
  在 `routes/api.php` 中設計了自定義的 `_roleCheck` 輔助函式，對敏感 API 進行即時角色過濾。角色體系涵蓋：
  - 臨床人員：`護理師`、`護理長`、`醫生`、`院長`
  - 管理人員：`醫院管理者`、`最高權限管理者`
* **資料庫支援**：預設採用 SQLite / MySQL，支持輕量化部署與容器化擴展。

### 2. 前端架構 (Vue 3 & Vite & Element Plus)
* **極速建置工具**：使用 **Vite** 進行前端打包與 HMR（熱更新）監聽，在 Docker 容器環境中實現快速響應。
* **UI 元件庫**：引進 **Element Plus**。其豐富的表單、表格與彈窗元件非常契合醫療病歷系統中高密度、多欄位資料登錄與即時校驗的場景。
* **狀態管理**：使用 **Pinia** 進行全域狀態管理（如當前登入使用者、診所配置、報到病患隊列等），確保跨頁面與元件的狀態同步。
* **現代化樣式**：採用最新版本的 **Tailwind CSS v4.0**，支持原子化 Class 撰寫，保持介面在各尺寸螢幕（如床邊平板、護理站大螢幕）的良好自適應。

---

## 🏥 二、 核心業務模組與工作流分析

血液透析是流程高度標準化、時間長且容錯率極低的醫療常規。本系統針對透析的「前、中、後」三個階段，設計了對應的閉環管理流程：

```text
 ┌──────────────┐      ┌────────────────────────┐      ┌────────────────────────┐      ┌──────────────┐
 │ 1. 病患報到  │ ───> │ 2. 透前整備 (雙人簽核) │ ───> │ 3. 透中觀測 & 護理記錄 │ ───> │ 4. 透析完成  │
 └──────────────┘      └────────────────────────┘      └────────────────────────┘      └──────────────┘
    - 體重測量            - 長短期機器 Prescription        - 定時生命徵象監測              - 脫水量核對
    - Hct 紅血球容積       - EPO / 鐵劑整備                 - 約束帶 / 保護設備評估          - 收針、衛教
    - 報到狀態確認         - 前置用藥核對                   - 護理片語快捷輸入              - 庫存扣減
```

### 1. 病患報到與基本量測 (Check-in & Vital Signs)
* **報到佇列管理**：支援病患進入診所後的報到（Check-ins），登錄病患在院或請假狀態。
* **體重差異管理**：登錄病患透析前體重，為計算本次「脫水量（超濾量）」提供精確基準。
* **初期生命徵象**：記錄意識狀態、皮膚完整性、呼吸與初步血壓。

### 2. 透前整備與安全防禦 (Pre-Dialysis Preparation)
* **機器與處方配置**：系統區分長、短期機器設定參數（如人工腎臟型號、治療時間、血流量、透析液流量、電導度、Heparin 肝素劑量等），並由 `PrepareController` 進行匹配。
* **雙重簽核機制 (Double Sign)**：
  依據醫療常規，高警訊藥物（如 EPO、鐵劑、肝素）在給藥前，必須由兩位醫護同仁進行雙重確認。系統對應實作了 `sign` 與 `double_sign` 簽章機制。
* **前置用藥管理**：管理透前給予的口服或針劑藥物。

### 3. 透析中觀測與護理記錄 (Intra-Dialysis Monitoring)
* **動態監測系統 (Monitoring)**：透析過程中，護理人員每半小時至一小時需巡房並登錄機器數值與病患生命徵象（靜脈壓、跨膜壓、血流量、血壓等）。
* **結構化護理病歷 (Nurse Record)**：
  提供結構化的病歷書寫面板，並配備**護理片語庫（Phrases）**，醫護人員能快速選擇常用描述，減少打字時間。
* **約束與安全護理**：
  針對意識不清、躁動或具跌倒風險之病患，系統依 N-019 護理評估常規，提供保護性約束評估（Protective Equipment & Restraint Check）。

### 4. 醫生臨床決策端 (Physician Portal)
* **SOAP 病歷**：醫生專屬介面，記錄病患的主觀敘述（Subjective）、客觀檢查（Objective）、評估（Assessment）與處置計畫（Plan）。
* **護理監控中樞 (Nurse Watching)**：提供醫生即時檢視所有正在透析中病患的警訊與狀態統計，促進醫護間的高效協作。

### 5. 後續排班與病床綁定 (Scheduling & IoT Ready)
* **病患班表（Schedule）**：管理病患每週一三五、二四六的固定洗腎班表與臨時請假、補洗排定。
* **護理師排床（Nurse-Bed Assignment）**：規劃病床、病患與負責護理師的對應關係。
* **床邊設備綁定 (Bed Binding)**：支持將特定硬體設備（Devices）與床位綁定，奠定未來自動擷取儀器數據的 IoT 基礎。

---

## 🛠️ 三、 核心代碼架構與領域模型 (Domain Models)

本專案擁有極富醫療專業度的資料模型，完美對應臨床領域知識：

* **`PatientCheck`**：透析主紀錄，串接病患的單次透析病歷、檢驗、用藥與監測。
* **`PatientBeforePreparation`**：儲存透前準備工作，包含耗材、管路與機器自我測試。
* **`PatientDialysisMachineLong` / `PatientDialysisMachineShort`**：分別記錄長期（常規）與短期（臨時）的透析機處方參數。
* **`PatientVascularAccessRecord`**：記錄血管通路（自體廔管/人工血管/雙腔導管）的穿刺點、血流量與廔管健康狀況。
* **`PatientEpoIronMedicine`**：追蹤紅血球生成素及鐵劑等貧血專用藥物的月度與週期施打進度。

---

## 🌟 四、 專案優勢與未來擴展建議

1. **高度的臨床合規性**：
   系統的雙人簽核、約束評估、血管通路、貧血 EPO 管理等完全契合洗腎診所的評鑑指標與日常流程，實用性極強。
2. **多重權限安全屏障**：
   採用 Sanctum + 強制路由層 RoleCheck 的雙重保障，防範未經授權的跨角色操作，對患者隱私起到極佳保護。
3. **物聯網 (IoT) 擴展潛力**：
   現有 API 已經預留了 BedBinding 與 Devices 路由。未來可直接對接床邊血壓計、電子體重計與透析機（如 Fresenius, Braun 等廠牌），實現數值自動帶入，徹底解放護理人員雙手。
4. **決策輔助 (CDS) 導入**：
   可基於 Monitoring 歷史數據，利用 Rule Engine 對異常血壓跌幅（如透析中低血壓/IDH）進行提前預警。
